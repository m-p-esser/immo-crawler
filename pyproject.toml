[project]
name = "immo-crawler"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "setuptools>=80.9.0",
]

[dependency-groups]
dev = [
    "apache-airflow>=3.0.1",
]
prod = [
    "crawlee[beautifulsoup,playwright]>=0.6.10",
]
